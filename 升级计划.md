# ModernWMS 整体升级 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将当前 ModernWMS 独立升级为以 .NET 10、MySQL 8.4、Vue 3.5、Vite 8、Vuetify 4 和最新版 VXE Table 4 为基础的可持续维护项目。

**Architecture:** 保留现有前后端分离结构和仓储业务模型，先建立自动化基线，再分别升级后端运行时、数据库、安全配置和前端依赖。每个阶段单独编译、测试和提交，只有当前阶段验收通过才进入下一阶段；不在同一提交中混合后端、Vuetify、VXE Table 和打印插件迁移。

**Tech Stack:** .NET SDK 10.0.302、ASP.NET Core 10.0.10、EF Core 10.0.9、MySql.EntityFrameworkCore 10.0.9、MySQL 8.4、Node.js 24.16.0、Vue 3.5.41、Vite 8.2.0、TypeScript 6.0.3、Vuetify 4.1.7、VXE Table 4.20.9、Vitest 4.1.10、Playwright 1.62.1。

## Global Constraints

- 开发分支固定为 `ruiyi`，`master` 只保留上游下载基线。
- 后端目标框架固定为 `net10.0`，仓库使用 `global.json` 选择 SDK `10.0.302` 并允许滚动到同一 .NET 10 的更高 feature band。
- 默认且唯一支持的数据库为本机 MySQL 8.4，数据库名固定为 `wms`。
- 数据库密码、JWT 密钥和其他敏感配置不得提交到 Git；本地使用环境变量或 .NET User Secrets。
- 前端固定使用 npm，只保留 `package-lock.json`，移除 `yarn.lock`。
- Vite 不支持项目绝对路径包含 `#`；正式开发前将仓库迁移到不含 `#` 的路径（建议 `D:\workspace\csharp\ModernWMS`）。当前升级验证临时通过 `subst W:` 映射执行。
- TypeScript 暂定 `6.0.3`，原因是当前 `@typescript-eslint` 8.66.0 尚不支持 TypeScript 7。
- 所有 Vue 单文件组件继续使用 Composition API、`<script setup>` 和 TypeScript。
- 每个阶段必须有独立测试或可重复的构建/冒烟命令，并形成独立 Git 提交。
- 不直接修改仓库自带的 `backend/ModernWMS/wms.db`；它只作为原始基础数据参考源。

---

## Phase 0：分支、版本锁定与原版基线

### Task 1：建立独立升级分支并锁定 SDK

**Files:**
- Create: `global.json`
- Verify: `backend/ModernWMS.sln`

**Interfaces:**
- Consumes: 当前 `master` 提交 `1837e17`。
- Produces: `ruiyi` 分支和统一的 .NET 10 SDK 选择规则。

- [x] **Step 1: 确认工作区无未识别改动**

Run:

```powershell
git status --short --branch
git log -1 --oneline
```

Expected: 当前为 `master`，基线提交为 `1837e17`，除本计划文件外没有源代码改动。

- [x] **Step 2: 创建并切换分支**

```powershell
git switch -c ruiyi
```

Expected: `Switched to a new branch 'ruiyi'`。

- [x] **Step 3: 创建 SDK 锁定文件**

Create `global.json`:

```json
{
  "sdk": {
    "version": "10.0.302",
    "rollForward": "latestFeature",
    "allowPrerelease": false
  }
}
```

- [x] **Step 4: 验证 SDK 选择**

```powershell
dotnet --version
dotnet --info
```

Expected: `dotnet --version` 输出 `10.0.302`。

- [x] **Step 5: 提交分支基线与计划**

```powershell
git add global.json 升级计划.md
git commit -m "chore: establish modernization baseline"
```

### Task 2：记录原项目的可重复基线

**Files:**
- Create: `docs/baseline.md`
- Verify: `backend/ModernWMS/Properties/launchSettings.json`
- Verify: `frontend/.env.development`

**Interfaces:**
- Consumes: 原始后端、前端和 SQLite 基础数据。
- Produces: 后续升级阶段可比较的启动端口、页面和基础数据清单。

- [x] **Step 1: 记录代码规模和原始版本**

```powershell
dotnet --list-sdks
node --version
npm --version
git status --short
```

- [x] **Step 2: 记录 SQLite 基础数据数量**

使用只读连接统计 `menu=19`、`rolemenu=19`、`user=1`、`userrole=1`，并将结果写入 `docs/baseline.md`。

- [x] **Step 3: 记录需要保持的业务入口**

`docs/baseline.md` 必须列出：登录、用户菜单、仓库、货主、SKU、ASN 入库、出库单、库存查询、库存调整、打印模板、Hangfire Dashboard 和 Swagger。

- [x] **Step 4: 提交基线文档**

```powershell
git add docs/baseline.md 升级计划.md
git commit -m "docs: capture legacy behavior baseline"
```

---

## Phase 1：自动化测试基础设施

### Task 3：建立 .NET 10 测试项目

**Files:**
- Create: `backend/ModernWMS.Tests/ModernWMS.Tests.csproj`
- Modify: `backend/ModernWMS.sln`

**Interfaces:**
- Consumes: `ModernWMS.Core` 和 `ModernWMS.WMS`。
- Produces: `dotnet test backend/ModernWMS.sln` 统一测试入口。

- [x] **Step 1: 创建 xUnit 测试项目**

```powershell
dotnet new xunit -n ModernWMS.Tests -o backend/ModernWMS.Tests -f net10.0
dotnet sln backend/ModernWMS.sln add backend/ModernWMS.Tests/ModernWMS.Tests.csproj
dotnet add backend/ModernWMS.Tests/ModernWMS.Tests.csproj reference backend/ModernWMS.Core/ModernWMS.Core.csproj backend/ModernWMS.WMS/ModernWMS.WMS.csproj
```

- [x] **Step 2: 删除模板占位测试**

删除 `backend/ModernWMS.Tests/UnitTest1.cs`；具体行为测试在对应生产改动前逐个加入，避免多个不存在类型互相遮蔽 RED 结果。

- [x] **Step 3: 验证测试项目能够加载解决方案引用**

```powershell
dotnet test backend/ModernWMS.Tests/ModernWMS.Tests.csproj
```

Expected: 测试项目成功还原和编译，输出 0 failed tests。

- [x] **Step 4: 提交测试基架**

```powershell
git add backend/ModernWMS.Tests backend/ModernWMS.sln
git commit -m "test: add modernization regression harness"
```

### Task 4：建立前端单元测试和浏览器冒烟入口

**Files:**
- Modify: `frontend/package.json`
- Create: `frontend/vitest.config.ts`
- Create: `frontend/src/utils/http/serverUrl.spec.ts`
- Create: `frontend/tests/smoke/login.spec.ts`
- Create: `frontend/playwright.config.ts`

**Interfaces:**
- Consumes: Vite 应用入口和 axios 请求封装。
- Produces: `npm run test:unit`、`npm run test:e2e`、`npm run build`。

- [x] **Step 1: 安装测试依赖**

```powershell
cd frontend
npm install -D vitest@0.34.6 @vitest/coverage-v8@0.34.6 @vue/test-utils@2.4.11 jsdom@22.1.0 playwright@1.62.1
```

Vitest `0.34.6` 仅作为 Vite 4 升级前的测试基线；Task 9 与 Vite 8 同步升级到 Vitest `4.1.10`。

执行记录：Vite 4 在包含 `#` 的绝对路径下无法加载 `/@vite/env`，因此实际执行时先完成 Task 9 的构建链升级，再通过 `subst W:` 获得可验证的失败测试；未使用路径错误充当业务测试红灯。

- [x] **Step 2: 添加 scripts**

```json
{
  "test:unit": "vitest run",
  "test:e2e": "playwright test",
  "test": "npm run test:unit && npm run build"
}
```

- [x] **Step 3: 写请求地址失败测试**

```ts
it('builds the API origin from VITE_BASE_PATH and VITE_SERVER_PORT', () => {
  expect(buildServerUrl('http://127.0.0.1', '21011')).toBe('http://127.0.0.1:21011')
})
```

Expected before implementation: FAIL，因为 `buildServerUrl` 尚未导出。

- [x] **Step 4: 写登录页浏览器冒烟测试**

```ts
test('login page renders account and password inputs', async ({ page }) => {
  await page.goto('/login')
  await expect(page.locator('input')).toHaveCount(2)
})
```

- [x] **Step 5: 提交前端测试基架**

```powershell
git add frontend/package.json frontend/package-lock.json frontend/vitest.config.ts frontend/src/utils/http/serverUrl.spec.ts frontend/tests frontend/playwright.config.ts
git commit -m "test: add frontend unit and browser smoke tests"
```

---

## Phase 2：后端升级到 .NET 10

### Task 5：升级目标框架并清理旧 ASP.NET 包（已完成）

**Files:**
- Modify: `backend/ModernWMS/ModernWMS.csproj`
- Modify: `backend/ModernWMS.Core/ModernWMS.Core.csproj`
- Modify: `backend/ModernWMS.WMS/ModernWMS.WMS.csproj`

**Interfaces:**
- Consumes: .NET SDK 10.0.302。
- Produces: 全部后端项目统一目标 `net10.0`。

- [x] **Step 1: 将三个 TargetFramework 改为 net10.0**

```xml
<TargetFramework>net10.0</TargetFramework>
```

- [x] **Step 2: 删除共享框架已经提供的 2.2 包**

从 `ModernWMS.Core.csproj` 删除：

```xml
<PackageReference Include="Microsoft.AspNetCore.Authentication" Version="2.2.0" />
<PackageReference Include="Microsoft.AspNetCore.Hosting" Version="2.2.7" />
<PackageReference Include="Microsoft.AspNetCore.Http" Version="2.2.2" />
<PackageReference Include="Microsoft.AspNetCore.Mvc" Version="2.2.0" />
```

- [x] **Step 3: 将 Microsoft 和基础依赖升级到明确版本**

使用：EF Core `10.0.9`（与 Oracle MySQL Provider 精确对齐）、JWT/NewtonsoftJson/OpenAPI `10.0.10`、Mapster `10.0.11`、NLog.Web.AspNetCore `6.1.4`、Newtonsoft.Json `13.0.4`、Swashbuckle `10.2.3`、Swashbuckle.Filters `10.0.1`、Hangfire `1.8.24`、Hangfire.MemoryStorage `1.8.1.2`。

- [x] **Step 4: 恢复并编译**

```powershell
dotnet restore backend/ModernWMS.sln
dotnet build backend/ModernWMS.sln --configuration Debug --no-restore
```

Expected: 0 errors；警告逐条分类，不能使用全局 `NoWarn` 隐藏。

- [x] **Step 5: 提交框架升级**

```powershell
git add backend/*.sln backend/**/*.csproj
git commit -m "build: upgrade backend projects to dotnet 10"
```

### Task 6：改为唯一 MySQL 8.4 Provider（已完成）

**Files:**
- Modify: `backend/ModernWMS.Core/ModernWMS.Core.csproj`
- Modify: `backend/ModernWMS.Core/Extentions/StartupExtensions.cs`
- Create: `backend/ModernWMS.Core/Configuration/DatabaseProvider.cs`
- Create: `backend/ModernWMS.WMS/Services/Stock/StockAgeCalculator.cs`
- Modify: `backend/ModernWMS.WMS/Services/Stock/StockService.cs`
- Test: `backend/ModernWMS.Tests/Configuration/DatabaseConfigurationTests.cs`
- Test: `backend/ModernWMS.Tests/Domain/StockAgeCalculatorTests.cs`

**Interfaces:**
- Consumes: `ConnectionStrings:MySqlConn`。
- Produces: 只使用 `MySql.EntityFrameworkCore 10.0.9` 的 `SqlDBContext` 和与数据库无关的 `StockAgeCalculator.Calculate(DateTime, DateTime)`。

- [x] **Step 1: 删除非 MySQL Provider 包并加入官方 MySQL Provider**

移除 Sqlite、SqlServer、Npgsql、Pomelo；加入：

```xml
<PackageReference Include="MySql.EntityFrameworkCore" Version="10.0.9" />
```

- [x] **Step 2: 运行数据库 Provider 测试，确认仍为 RED**

```powershell
dotnet test backend/ModernWMS.Tests/ModernWMS.Tests.csproj --filter Database_provider_must_be_mysql
```

- [x] **Step 3: 实现唯一 Provider 常量与 UseMySQL 注册**

```csharp
public static class DatabaseProvider
{
    public const string Name = "MySql";
}
```

注册只保留：

```csharp
var connectionString = configuration.GetConnectionString("MySqlConn")
    ?? throw new InvalidOperationException("ConnectionStrings:MySqlConn is required.");
options.UseMySQL(connectionString);
```

- [x] **Step 4: 运行库存库龄测试，确认仍为 RED**

```powershell
dotnet test backend/ModernWMS.Tests/ModernWMS.Tests.csproj --filter Stock_age_uses_calendar_days
```

- [x] **Step 5: 实现通用库龄计算并替换多 Provider 分支**

```csharp
public static int Calculate(DateTime putawayDate, DateTime today) =>
    Math.Max(0, (today.Date - putawayDate.Date).Days);
```

- [x] **Step 6: 运行全量后端测试与编译**

```powershell
dotnet test backend/ModernWMS.sln
dotnet build backend/ModernWMS.sln --configuration Release
```

- [x] **Step 7: 提交 MySQL 收敛**

```powershell
git add backend
git commit -m "refactor: standardize persistence on mysql 8.4"
```

### Task 7：现代化启动、端口和配置安全

**Files:**
- Modify: `backend/ModernWMS/Program.cs`
- Remove: `backend/ModernWMS/Startup.cs`
- Modify: `backend/ModernWMS/appsettings.json`
- Modify: `backend/ModernWMS/appsettings.Development.json`
- Modify: `backend/ModernWMS/Properties/launchSettings.json`
- Modify: `backend/ModernWMS.Core/Extentions/StartupExtensions.cs`
- Remove: `backend/ModernWMS.Core/Middleware/CorsMiddleware.cs`
- Modify: `backend/ModernWMS.Core/JWT/TokenManager.cs`
- Create: `backend/ModernWMS.Tests/Security/TokenValidationTests.cs`
- Create: `backend/ModernWMS.Tests/Security/TokenTestFactory.cs`
- Create: `backend/ModernWMS.Tests/Hosting/ApplicationStartupTests.cs`

**Interfaces:**
- Consumes: `ConnectionStrings__MySqlConn`、`TokenSettings__SigningKey`、`ASPNETCORE_URLS`。
- Produces: 标准 WebApplication 启动、受控 CORS、有效期强制校验和统一端口 `21011`。

- [x] **Step 1: 写过期 Token 失败测试**

```csharp
[Fact]
public void Expired_token_is_rejected()
{
    Assert.Throws<SecurityTokenExpiredException>(
        () => TokenTestFactory.ValidateExpiredToken());
}
```

Expected before implementation: FAIL，因为当前代码关闭了生命周期校验。

- [x] **Step 2: 将 Program 改为 WebApplication 模式**

保留现有扩展注册，但由 `WebApplication.CreateBuilder(args)`、`builder.Services` 和 `app.Run()` 负责生命周期；移除硬编码 `UseUrls("http://*:5555")`。

- [x] **Step 3: 统一端口**

`launchSettings.json`、前端 `.env.development`、Docker 和文档统一使用后端端口 `21011`，本机前端端口固定 `80`。

- [x] **Step 4: 清除仓库中的真实凭据默认值**

`appsettings.json` 中连接串改为空字符串，JWT SigningKey 不再包含默认密钥；启动时缺少配置必须抛出清晰错误。

- [x] **Step 5: 使用 ASP.NET Core CORS Policy 替代回显 Origin 的中间件**

开发环境允许 `http://localhost`、`http://127.0.0.1`、`http://localhost:80` 和 `http://127.0.0.1:80`；生产环境从 `Cors:AllowedOrigins` 读取白名单。

- [x] **Step 6: 恢复 Token 生命周期校验**

所有手工 `ValidateToken` 路径设置：

```csharp
ValidateAudience = true,
ValidateIssuer = true,
ValidateIssuerSigningKey = true,
ValidateLifetime = true,
ClockSkew = TimeSpan.Zero
```

- [x] **Step 7: 仅在 Development 开启敏感数据日志**

生产环境不得调用 `EnableSensitiveDataLogging()`。

- [x] **Step 8: 运行安全测试与后端全量测试**

```powershell
dotnet test backend/ModernWMS.sln
```

- [x] **Step 9: 提交启动和安全治理**

```powershell
git add backend frontend/.env.development
git commit -m "security: modernize hosting and configuration"
```

---

## Phase 3：MySQL 8.4 建库、Migration 与初始化

### Task 8：创建 MySQL Migration 和确定性种子数据

**Files:**
- Create: `backend/ModernWMS/Migrations/*_InitialMySql.cs`
- Create: `backend/ModernWMS/Migrations/SqlDBContextModelSnapshot.cs`
- Create: `backend/ModernWMS/SeedData/menu.json`
- Create: `backend/ModernWMS/SeedData/rolemenu.json`
- Create: `backend/ModernWMS/SeedData/user.json`
- Create: `backend/ModernWMS/SeedData/userrole.json`
- Create: `backend/ModernWMS/Initialization/DatabaseInitializer.cs`
- Create: `backend/ModernWMS/Initialization/DesignTimeSqlDbContextFactory.cs`
- Create: `backend/ModernWMS/Initialization/SeedManifest.cs`
- Create: `backend/ModernWMS.Tests/Database/SeedDataTests.cs`

**Interfaces:**
- Consumes: 原始只读 `wms.db` 中 19 条菜单、19 条角色菜单、1 个用户、1 个用户角色。
- Produces: 可重复创建 MySQL `wms` 数据库结构并幂等初始化基础数据的 `DatabaseInitializer.InitializeAsync()`。

- [x] **Step 1: 将四类基础数据从 SQLite 导出为固定 JSON**

导出过程只读原库；JSON 按主键排序，确保重复导出内容一致。

- [x] **Step 2: 写种子数量失败测试**

```csharp
[Fact]
public async Task Seed_manifest_contains_legacy_minimum_data()
{
    var manifest = await SeedManifest.LoadAsync();
    Assert.Equal(19, manifest.Menus.Count);
    Assert.Equal(19, manifest.RoleMenus.Count);
    Assert.Single(manifest.Users);
    Assert.Single(manifest.UserRoles);
}
```

Expected before implementation: FAIL，因为 `SeedManifest` 尚不存在。

- [x] **Step 3: 生成 MySQL 初始 Migration**

```powershell
dotnet ef migrations add InitialMySql --project backend/ModernWMS --startup-project backend/ModernWMS --context SqlDBContext
```

- [x] **Step 4: 实现幂等初始化器**

顺序固定为：`MigrateAsync()` → user → menu → userrole → rolemenu；已存在主键时不重复插入。

> Oracle MySQL Provider 10.0.9 生成的 `--idempotent` 脚本包含不适用于 MySQL 的通用控制块；部署统一使用应用内 `MigrateAsync()` 或 `dotnet ef database update`。普通 Migration SQL 已验证可生成 36 张表和 8 个索引。

- [x] **Step 5: 配置本机密钥**

```powershell
dotnet user-secrets init --project backend/ModernWMS
dotnet user-secrets set "ConnectionStrings:MySqlConn" "Server=127.0.0.1;Port=3306;Database=wms;User ID=modernwms_app;Password=<local-secret>;Character Set=utf8mb4;" --project backend/ModernWMS
dotnet user-secrets set "TokenSettings:SigningKey" "<local-64-byte-secret>" --project backend/ModernWMS
```

尖括号内容只存在于本机 User Secrets，不写入仓库或日志。

- [x] **Step 6: 在本机 MySQL 创建数据库和最小权限账号**

```sql
CREATE DATABASE IF NOT EXISTS wms CHARACTER SET utf8mb4 COLLATE utf8mb4_0900_ai_ci;
CREATE USER IF NOT EXISTS 'modernwms_app'@'localhost' IDENTIFIED BY '<local-secret>';
GRANT SELECT, INSERT, UPDATE, DELETE, CREATE, ALTER, INDEX, DROP, REFERENCES ON wms.* TO 'modernwms_app'@'localhost';
FLUSH PRIVILEGES;
```

- [x] **Step 7: 应用 Migration 并验证表和基础数据**

```powershell
dotnet ef database update --project backend/ModernWMS --startup-project backend/ModernWMS
dotnet test backend/ModernWMS.Tests/ModernWMS.Tests.csproj --filter Seed
```

也可单独执行初始化并自动退出：

```powershell
dotnet run --project backend/ModernWMS -- --initialize-database-only
```

本机 MySQL 8.4.8 实测：36 张 InnoDB 表、`utf8mb4_0900_ai_ci`、基础数据 `19/19/1/1`；连续初始化两次数量不变。

- [x] **Step 8: 提交数据库初始化能力**

```powershell
git add backend/ModernWMS/Migrations backend/ModernWMS/SeedData backend/ModernWMS.Core/DBContext backend/ModernWMS.Tests/Database
git commit -m "feat: initialize mysql schema and baseline data"
```

---

## Phase 4：前端构建链升级

### Task 9：统一 npm、Node 24、Vue 3.5 和 Vite 8

**Files:**
- Modify: `frontend/package.json`
- Modify: `frontend/package-lock.json`
- Remove: `frontend/yarn.lock`
- Modify: `frontend/vite.config.ts`
- Modify: `frontend/tsconfig.json`
- Modify: `frontend/tsconfig.node.json`

**Interfaces:**
- Consumes: Node.js `24.16.0` 和 npm 11。
- Produces: Vue `3.5.41`、Vite `8.2.0`、TypeScript `6.0.3`、vue-tsc `3.3.9` 的可重复构建。

- [x] **Step 1: 在 package.json 固定 engines 和 packageManager**

```json
{
  "engines": { "node": ">=24.16.0" },
  "packageManager": "npm@11.17.0"
}
```

- [x] **Step 2: 升级核心构建依赖**

```powershell
npm install vue@3.5.41 vite@8.2.0 typescript@6.0.3 vue-tsc@3.3.9 @vitejs/plugin-vue@6.0.8
npm install -D vitest@4.1.10 @vitest/coverage-v8@4.1.10 jsdom@30.0.1
```

- [x] **Step 3: 将 TypeScript/ESLint 组合保持在兼容范围**

使用 ESLint `10.8.0`、`@typescript-eslint/parser` 和 `@typescript-eslint/eslint-plugin` `8.66.0`；在其支持 TypeScript 7 前不升级到 TypeScript 7。

- [x] **Step 4: 运行单元测试、类型检查和构建**

```powershell
npm run test:unit
npm run build
```

- [x] **Step 5: 提交构建链升级**

```powershell
git add frontend
git commit -m "build: upgrade vue and vite toolchain"
```

### Task 10：升级 Router、i18n、状态管理和 HTTP 层（已完成）

**Files:**
- Modify: `frontend/src/router/**`
- Modify: `frontend/src/languages/**`
- Modify: `frontend/src/store/**`
- Modify: `frontend/src/utils/http/request.ts`
- Modify: `frontend/src/main.ts`
- Test: `frontend/src/utils/http/request.spec.ts`

**Interfaces:**
- Consumes: 现有 token、菜单、语言和路由状态。
- Produces: Vue Router 5.2.0、vue-i18n 11.4.8、Pinia 4.0.2、axios 1.19.0。

- [x] **Step 1: 先保持现有 Vuex 行为写状态测试**

覆盖登录 token 持久化、语言、打开菜单和退出清理四种行为，测试必须在替换 Vuex 前通过。

- [x] **Step 2: 升级 Router、i18n 和 axios**

```powershell
npm install vue-router@5.2.0 vue-i18n@11.4.8 axios@1.19.0
```

- [x] **Step 3: 用 Pinia 替换 Vuex 与 vuex-persist**

```powershell
npm install pinia@4.0.2
npm uninstall vuex vuex-persist
```

Store 边界保持为 `user`、`system` 两个领域；组件不得直接读写 localStorage。

- [x] **Step 4: 让请求地址构造测试转为 GREEN**

导出纯函数：

```ts
export const buildServerUrl = (basePath: string, port: string) =>
  `${basePath.replace(/\/$/, '')}:${port}`
```

- [x] **Step 5: 运行测试和构建**

```powershell
npm run test
```

- [x] **Step 6: 提交应用基础库升级**

```powershell
git add frontend
git commit -m "refactor: modernize router state and http stack"
```

---

## Phase 5：Vuetify 4 与 VXE Table 升级

### Task 11：迁移到 Vuetify 4（已完成）

**Files:**
- Modify: `frontend/src/plugins/vuetify/**`
- Modify: `frontend/src/style.css`
- Modify: `frontend/src/**/*.vue`
- Modify: `frontend/vite.config.ts`
- Test: `frontend/tests/smoke/login.spec.ts`
- Create: `frontend/tests/smoke/navigation.spec.ts`

**Interfaces:**
- Consumes: 140 个使用 Vuetify 标签的组件。
- Produces: Vuetify `4.1.7` 与 vite-plugin-vuetify `2.1.3`，保持原有页面导航和表单行为。

- [x] **Step 1: 保存升级前关键页面截图**

页面集合固定为：登录、首页、用户、仓库、SKU、ASN、出库、库存、打印模板。

- [x] **Step 2: 安装 Vuetify 4**

```powershell
npm install vuetify@4.1.7 vite-plugin-vuetify@2.1.3
```

- [x] **Step 3: 先修复插件入口与全局样式**

保持默认主题为 light，并显式恢复项目所依赖的旧断点阈值，避免一次升级造成全站布局漂移。

- [x] **Step 4: 按组件类别迁移**

顺序固定为：布局 → 按钮/图标 → 表单字段 → Dialog/Drawer → Select/Autocomplete → Data/Date 输入 → 主题和排版类。每一类完成后运行 `npm run test`。

- [x] **Step 5: 执行浏览器回归**

```powershell
npm run test:e2e
```

Expected: 九个关键页面可访问，无 Vue warning、无控制台 error，表单标签和按钮未遮挡。

- [x] **Step 6: 提交 Vuetify 升级**

```powershell
git add frontend
git commit -m "feat: migrate user interface to vuetify 4"
```

### Task 12：升级 VXE Table 与导出插件

**Files:**
- Modify: `frontend/src/plugins/VXETable/**`
- Modify: `frontend/src/components/table/**`
- Modify: 72 个包含 `<vxe-*>` 的 Vue 文件
- Create: `frontend/tests/smoke/table-workflows.spec.ts`

**Interfaces:**
- Consumes: 当前分页、筛选、编辑、校验、导入和导出行为。
- Produces: VXE Table `4.20.9`、xe-utils `4.0.12`、vxe-table-plugin-export-xlsx `4.0.7`。

- [x] **Step 1: 编写表格流程浏览器测试**

至少覆盖：分页、筛选、单元格编辑、弹窗表格、Excel 导出按钮和导入预览。

- [x] **Step 2: 升级依赖**

```powershell
npm install vxe-table@4.20.9 xe-utils@4.0.12 vxe-table-plugin-export-xlsx@4.0.7
```

- [x] **Step 3: 修复公共插件和公共列组件**

先修复 `plugins/VXETable` 与 `vxe-date-column.vue`，再处理业务页面；禁止在 72 个页面复制同一兼容补丁。

- [x] **Step 4: 运行表格专项回归**

```powershell
npm run test:e2e -- table-workflows.spec.ts
npm run build
```

- [x] **Step 5: 提交 VXE Table 升级**

```powershell
git add frontend
git commit -m "功能：升级 VXE Table 与导出组件，验证：表格专项冒烟、类型检查和生产构建通过"
```

### Task 13：处理打印、二维码、图表和旧插件

**Files:**
- Modify: `frontend/src/components/hiprint/**`
- Modify: `frontend/src/view/base/print/**`
- Modify: `frontend/src/components/codeDialog/**`
- Modify: `frontend/src/view/largeScreen/**`
- Modify: `frontend/package.json`
- Create: `frontend/tests/smoke/print-and-code.spec.ts`

**Interfaces:**
- Consumes: Hiprint 模板、条码、二维码、Excel 和大屏图表功能。
- Produces: 在新构建链上可运行且有回归覆盖的外围功能。

- [x] **Step 1: 为打印预览、条码和二维码建立浏览器测试**

- [x] **Step 2: 保留无新版的插件版本并建立隔离适配器**

`yk-vue-plugin-hiprint 0.0.9` 和 `vue3-print-nb 0.1.4` 没有更新版；通过 `src/adapters/printing` 封装，业务组件不再直接调用插件全局对象。

- [x] **Step 3: 升级仍维护的外围依赖**

升级 `qrcode.vue 3.10.0`、`vue-qr 5.0.3`、`exceljs 4.4.0`、`echarts 6.1.0`；jQuery 暂不升到 4，先消除三处直接依赖后再移除。

- [x] **Step 4: 运行专项测试和生产构建**

```powershell
npm run test:e2e -- print-and-code.spec.ts
npm run build
```

- [x] **Step 5: 提交外围插件治理**

```powershell
git add frontend
git commit -m "功能：隔离打印插件并升级可视化依赖，验证：打印二维码专项冒烟与生产构建通过"
```

---

## Phase 6：部署、全链路验收与交付

### Task 14：完善 Windows 原生运行配置

**Files:**
- Modify: `backend/ModernWMS.Core/Extentions/StartupExtensions.cs`
- Modify: `backend/ModernWMS.Tests/Hosting/ApplicationStartupTests.cs`
- Modify: `frontend/src/utils/http/serverUrl.ts`
- Modify: `frontend/src/utils/http/serverUrl.spec.ts`

**Interfaces:**
- Consumes: 本机 .NET 10 SDK、Node 24 和 MySQL 8.4。
- Produces: 可直接在 Windows 启动、检查和发布的后端与前端产物；本项目不使用 Docker。

- [x] **Step 1: 添加应用健康检查**

后端在数据库初始化成功并启动后暴露 `/health`，用于进程存活检查，并由启动测试覆盖；该端点本身不重复查询数据库。

- [x] **Step 2: 保持开发与发布地址兼容**

本机开发继续连接 `http://127.0.0.1:21011`；地址构造器同时支持无端口路径，便于后续接入 IIS 或其他反向代理。

- [x] **Step 3: 验证 Windows 原生发布产物**

```powershell
dotnet publish backend/ModernWMS/ModernWMS.csproj --configuration Release --output artifacts/backend
cd frontend
npm run build
```

- [x] **Step 4: 验证本机健康端点**

后端使用 User Secrets 中的本机 MySQL 连接启动后，访问 `http://127.0.0.1:21011/health` 应返回 `Healthy`。

- [x] **Step 5: 提交原生运行配置**

```powershell
git add backend frontend 升级计划.md
git commit -m "功能：完善 Windows 原生运行健康检查，验证：后端测试与前端单测通过"
```

### Task 15：执行最终验收并更新文档

**Files:**
- Modify: `README.md`
- Modify: `README.zh_CN.md`
- Create: `docs/development.md`
- Create: `docs/database.md`
- Create: `docs/deployment.md`
- Modify: `升级计划.md`

**Interfaces:**
- Consumes: 全部升级阶段产物。
- Produces: 可从空环境搭建、测试和部署的独立 ModernWMS 项目。

- [x] **Step 1: 后端验收**

```powershell
dotnet restore backend/ModernWMS.sln
dotnet build backend/ModernWMS.sln --configuration Release --no-restore
dotnet test backend/ModernWMS.sln --configuration Release --no-build
```

Expected: 0 errors、0 failed tests。

- [x] **Step 2: 前端验收**

```powershell
cd frontend
npm ci
npm run test:unit
npm run build
npm run test:e2e
```

Expected: 单元测试、类型检查、生产构建和浏览器冒烟全部通过。

- [x] **Step 3: MySQL 数据验收**

确认 Migration 已应用，基础账号、19 条菜单、19 条角色菜单和 1 条用户角色关系存在；重复启动不会重复插入。

验收结果：连续初始化两次后共有 36 张表、1 条 Migration，种子数量保持 `19/19/1/1`。

- [x] **Step 4: 业务验收**

逐项验证登录、用户菜单、仓库、货主、SKU、ASN 入库、出库单、库存查询、库存调整、打印模板、Hangfire Dashboard 和 Swagger。

验收结果：浏览器冒烟覆盖登录、菜单及关键业务页面；Windows 原生后端实测健康检查、Swagger、Hangfire Dashboard 和真实 MySQL 登录均返回成功。

- [x] **Step 5: 更新文档和计划状态**

把完成项勾选，并在 `docs/development.md` 写入 SDK、Node、MySQL、User Secrets、启动和测试命令。

- [x] **Step 6: 最终工作区确认**

```powershell
git status --short
git log --oneline --decorate -15
```

Expected: 所有预期文件已提交，工作区干净，每个阶段都有独立提交。

---

## 阶段验收与回退规则

1. 任何阶段出现无法解释的业务差异，停止进入下一阶段，使用该阶段前一个提交作为比较基线。
2. 数据库结构变更必须先生成 Migration SQL 并审阅，再应用到本机 MySQL。
3. Vuetify 和 VXE Table 不在同一个提交中升级。
4. 打印和导出属于独立高风险功能，必须保留浏览器测试和人工预览。
5. 不以“能够编译”代替完成；每个阶段至少满足编译、自动化测试、关键页面无控制台错误三项要求。
