# 发货流程（WMS 接管深圳自建仓 FBA 直接发货）

> 来源：WMS 交接 Memory `df838203-7ede-49d3-8986-4bab770fcc60`（WMS 接管深圳仓 FBA 核对发货流程）
> 关联：`061a7ed3-8b85-4574-81c5-050fe9e1da50`（WMS 接管深圳自建仓收货，上下游仓库身份及同库集成背景）
> 状态：第一阶段“发货单列表与库存核对展示”已实现；确认、称重和出库事务待后续阶段实现

## 一、背景与目标

ERP 在“物流发货 -> 发货信息 -> 国际货件”中维护 FBA 出海待发货准备单。业务决定：深圳自建仓（ERP warehouseId / fromWarehouseId = **320118**，展示名“深圳自建仓”，历史名“有座山深圳仓”）的 FBA 货件，后续由 ModernWMS 完成核对和实际发货，并由 WMS 在确认发货时完成**唯一一次**库存扣减；ERP 不再人工重复执行同一套核对/发货动作。

简化原则：

- 不需要冗余数据信息，只取“发货单（FBA 货件）”及其关键信息。
- 发货单可**直接读取 FBA 货件号**及内部信息，无需重建货件主数据。
- 只有 ERP 中“加入需发货”且“仓库是我们自己的仓库（深圳自建仓 320118）”的 FBA 货件，才是我们需要的货件号。
- 已有 FBA 货件信息 + 仓库库存信息，确认后**直接称重、发货（出库）**。
- 中间状态一律省略：旧系统的**预发货、新发货、待拣货、已拣货、打包**全部跳过，从“发货单确认”直接进入“称重”环节。

## 二、流程总览（目标流程）

```
ERP（FBA 货件/发货单，深圳仓 320118）
        │  读取发货单（FBA 货件号 fbaNo + 货件信息）
        ▼
WMS 发货列表（只展示深圳仓需发货的 FBA 货件）
        │  发货单中确认
        ▼
核对/关联库存（确认库存数据，SKU 一致性与数量）
        ▼
选择发货商（承运商）
        ▼
称重
        ▼
出库（唯一一次库存扣减，写发货记录）
```

注意：发货单确认 -> 称重 -> 出库之间，**不经过** 预发货/新发货/待拣货/已拣货/打包 等中间环节。发货单确认只进入待称重状态；完成称重并执行出库事务后，才进入统一“已发货/在途”口径。

## 三、数据来源与关键字段（后续取数直接使用）

### 1. ERP FBA 货件头（发货单主表）

| 字段 | 说明 / 建议命名 |
| --- | --- |
| id | fbaShipmentId，ERP FBA 货件主键 |
| amazonShipmentId | **FBA 货件号，跨系统业务唯一键，WMS 建议字段名 fbaNo** |
| name | 货件名称 |
| shopId / shopName | 发货店铺 |
| sellingPartnerId | 卖方身份 |
| marketplaceId / marketplaceName / region | 站点/市场 |
| shipmentStatus | Sellfox / Amazon 货件状态 |
| fulfillmentCenterId | Amazon 目的仓编码 |
| sourceAddrId / destAddrId | 发件/收件地址主键 |
| quantity / boxQuantity / cartonNum | 数量、箱数 |
| shippingMode / shippingSolution | 运输方式/方案 |
| creator | 订单归属运营用户 |
| deptId | 订单归属小组/部门 |

收件地址（按 destAddrId 关联 FBA 地址）：

- name、companyName、phoneNumber、email
- addressLine1、addressLine2、city、districtOrCounty、stateOrProvinceCode、postalCode、countryCode

### 2. FBA 货件明细（发货商品）

| 字段 | 说明 |
| --- | --- |
| id | fbaShipmentItemId |
| shipmentId | 所属货件 |
| commodityId / commoditySku / commodityName / mainImage | 商品主数据 + **商品图片** |
| msku / fnSku / asin / childSku / title | FBA 各 SKU 口径 |
| quantity | 计划发货数量 |
| boxQuantity / caseNum / quantityInCase | 箱相关 |
| outerBoxLength / outerBoxWidth / outerBoxHeight / outerBoxWeight | 外箱尺寸/重量（称重参考） |
| labelOwner / prepOwner / prepCategory / prepType | 标签/预处理 |

### 3. 箱唛 / 追踪

| 字段 | 说明 |
| --- | --- |
| id / shipmentId / boxId / idx | 箱唛标识 |
| trackingId | 箱唛追踪号 |
| trackingNumberValidationStatus | 追踪号校验状态 |
| weight / weightUnit / length / width / height / lengthUnit / dimension / status | 重量尺寸与状态 |

### 4. ERP 准备单（发货单落库事实）

- POST /erp/logistics-provider/stock/fba-shipment/prepare（兼容旧路径 …/ship，实际只调用 prepare，不代表真实出库）
- 请求头字段：stockId、warehouseId、fbaShipmentId、fbaNo、assistShipmentConfirmed
- items：fbaShipmentItemId、stockSku、fbaSku、qty、variantQty、shipmentTotalQty、skuMatched、skuMismatchConfirmed
- **shipmentTotalQty 必须等于 qty × variantQty**；qty 是 FBA 明细进度数量，shipmentTotalQty 才是库存锁定/扣减数量
- 准备时校验 SKU，不一致必须人工确认
- 准备时 ERP 将 trk_stock.available_qty 减少、occupied_qty 增加（冻结库存），并创建 trk_stock_move + trk_stock_move_item，状态 WAIT_SHIPMENT、transferType=OVERSEA_FBA_SHIPMENT
- 发货人/发货小组/来源仓库来自库存归属：orderUserId/orderUserName、deptId/deptName、fromWarehouseId/fromWarehouseName
- 同一 FBA 货件已有准备单时阻止重复准备

### 5. 仓库与库存归属

- 深圳自建仓：**warehouseId / fromWarehouseId = 320118**（过滤必须以此为准，名称仅展示/历史兼容，禁止按“深圳”模糊匹配）
- ERP 已有接口：
  - GET /erp/fba-shipment/page（FBA 货件分页）
  - GET /erp/fba-shipment/get?id={fbaShipmentId}（货件详情）
  - GET /erp/fba-shipment/fba-shipment-item/page?shipmentId={id}&pageNo=1&pageSize=50（货件商品）
  - GET /erp/fba-shipment/fba-spd-box/page?shipmentId={id}&pageNo=1&pageSize=50（箱唛/追踪号）
  - POST /erp/fba-shipment/refresh-sellfox-by-no（按 fbaNo 刷新 Sellfox 状态和箱唛）
  - POST /erp/logistics-provider/dispatch/shipment/fba-oversea-track/query（注册/核验海外追踪并推进状态）

## 四、发货单展示内容

列表至少展示：

- fbaNo（FBA 货件号）、货件名称、Sellfox/Amazon 状态、Amazon 目的仓
- shopName、marketplaceName
- 发货小组 deptId/deptName、发货人 orderUserId/orderUserName
- 来源仓库 fromWarehouseId/fromWarehouseName
- 产品数、shipmentTotalQty 合计、boxQuantity
- 主追踪号/箱唛数量、准备时间、状态

详情至少展示：

- 完整收件信息
- 每个库存产品：stockMoveItemId、stockId、commodityId、商品图片、商品名、stockSku
- 对应 FBA 明细：fbaShipmentItemId、fbaSku/MSKU/FNSKU/ASIN、qty、variantQty、shipmentTotalQty、SKU 是否一致
- 箱唛：boxId、idx、trackingId、重量和尺寸
- 发货人、发货小组、店铺、站点、Amazon 目的仓
- 版本号/更新时间（并发控制）

## 五、发货操作流程（简化后）

1. **读取发货单**：按 fbaNo 读取 FBA 货件头、明细、箱唛/追踪、收件信息（数据来自 ERP 共享库）。
2. **过滤**：只操作“来源仓库 = 320118（深圳自建仓）”且有有效 WAIT_SHIPMENT 准备单的货件；非 320118、已取消、已在途、已完成或没有有效准备单的记录不得出现在待发货列表。
3. **发货单确认**：在列表中确认该发货单（一次性确认，不做多阶段流转）。
4. **关联/核对库存**：确认库存 SKU 与 FBA SKU、数量（shipmentTotalQty = qty × variantQty），SKU 不一致必须显式确认；确认成功即完成“待发货”阶段。
5. **选择发货商**：选择承运商/发货商。
6. **称重**：录入/确认重量（称重环节）。
7. **出库**：唯一一次库存扣减（occupied_qty 减 shipmentTotalQty，total_qty 同步减，available_qty 不再减），写唯一出库流水，状态推进到统一“已发货/在途”，写主追踪号与操作记录。

### 事务与一致性（关键约束）

- 一个数据库事务内：锁定 trk_stock_move 及明细 -> 再校验 fromWarehouseId=320118、WAIT_SHIPMENT、未取消未出库 -> 校验 fbaShipmentId/fbaNo 与准备单快照一致 -> 校验明细唯一、SKU 关系、合计数与冻结数一致 -> 扣减冻结库存 -> 写唯一出库流水（业务键：stockMoveId + stockMoveItemId + stockId，重复请求不得再次扣）-> 推进状态并写 WMS 操作记录。
- 主追踪号必须校验；箱唛追踪号去重；追踪注册失败时整个发货动作不得标为成功。
- 幂等：重复点击、并发、超时重试必须返回同一成功结果，不重复扣库存。
- WMS 是深圳自建仓实际发货和库存扣减的**唯一写入方**；ERP 对 320118 准备单应隐藏/禁用会改库存或状态的动作，禁止双写。
- 取消/撤销只能发生在真实扣减前；扣减后不得简单“改回待发货”，必须走审计化冲销/退库。

## 六、建议接口（供实现参考）

- 已实现列表：`POST /fba-shipment/page`，只读返回深圳仓待发货准备单及其商品、库存和箱唛摘要。
- 详情：按 stockMoveId 或 fbaNo 获取完整信息
- 确认发货：`POST /api/fba-shipment/{stockMoveId}/check-and-ship`
  - stockMoveId、sourceVersion/updateTime（乐观锁）、fbaShipmentId、fbaNo（双重绑定校验）
  - items[]：stockMoveItemId、stockId、fbaShipmentItemId、stockSku、fbaSku、qty、variantQty、shipmentTotalQty、skuMatched、skuMismatchConfirmed
  - primaryTrackingNo、boxTrackingList[]（idx、boxId、trackingId）
  - shipperUserId、shipperName、shipmentTime、remark
  - idempotencyKey（建议 `WMS-FBA-SHIP-{stockMoveId}`）
- 状态推进：确认后进入待称重；称重并完成出库后统一进入“已发货/在途”口径，跳过 新发货/待拣货/已拣货/打包 等中间状态。

## 七、第一阶段实际落地口径

- 前端发货管理保留“FBA 发货单、称重、出库、已签收”四个入口，主流程为“发货单确认 -> 称重 -> 出库”，出库后的签收记录继续保留查询。旧的预发货/新发货/待拣货/已拣货/打包页签不再展示。
- 后端已映射 `trk_stock_move`、`trk_stock_move_item`、`trk_stock`、`erp_fba_shipment` 和 `erp_fba_spd_box`，列表以 ERP 准备单为事实来源，不直接把所有 FBA 货件当成仓库待发货任务。
- 列表固定筛选：`trk_stock_move.deleted=0`、`transfer_type='OVERSEA_FBA_SHIPMENT'`、`status='WAIT_SHIPMENT'`、`shipment_status='WAIT_SHIPMENT'`、`from_warehouse_id=320118`。
- 商品行由 `trk_stock_move_item.product_snapshot_json` 读取 FBA 与库存关联键、商品图片、SKU 和数量，再关联 `trk_stock` 核对冻结库存、关联 FBA 货件头及箱唛表补齐展示字段。
- 当前共享数据库按上述完整条件查询结果为 **0 条**。因此页面暂无数据是正确结果；ERP 后续加入符合条件的深圳仓“需发货”准备单后会自动展示。
- 第一阶段接口和页面均为只读，不修改 ERP 状态、不扣减库存、不写出库流水。发货单确认、发货商选择、称重和最终出库将作为后续阶段，以同一事务和幂等键实现。

- 数据库侧核实：预发货/新发货/待拣货/已拣货/打包在旧 WMS 中**没有独立数据表**，只是 `wms_dispatchlist.dispatch_status` 的 0-4 状态值，并不存在五张可单独删除的流程表。
- `wms_dispatchlist` 与 `wms_dispatchpicklist` 同时被当前保留的称重、出库、已签收接口使用，不能在这些接口完成 FBA 化改造前删表。两张表当前均为 0 行；流程配置表 `wms_flowset*` 与 `wms_menu.menu_actions` 也为空，因此本阶段没有旧流程数据或配置记录需要删除。
- 后续实现 FBA 发货单确认、称重和出库时，不再产生旧的 0-4 状态。待保留流程全部切换到新的 FBA 发货数据契约后，再删除不再被引用的旧字段或共用表，禁止为了删除旧状态而先破坏称重和出库。

## 八、验收标准（摘要）

1. 列表只出现深圳仓 320118 且有效 WAIT_SHIPMENT 的货件；非 320118 即使伪造请求也被后端拒绝。
2. 列表/详情完整展示：发货人、发货小组、店铺、收件信息、FBA 货件号、Amazon 目的仓、库存 SKU、FBA SKU/变体、数量、箱唛和追踪号。
3. 确认 -> 称重 -> 出库之间无中间阶段；发货成功只扣一次冻结库存，存在唯一出库流水。
4. 重复请求/双击/并发不重复扣库存；失败保留可读原因（仓库不符、状态已变化、库存冻结不一致、SKU 不一致未确认、追踪号缺失/重复、版本冲突）。
5. 发货成功后 ERP 与 WMS 展示同一在途/已发货状态和同一主追踪号。
6. WMS 实现完成后回写 Memory：实际 API、表结构、状态映射、幂等键、库存 SQL/事务边界和验收结果。
